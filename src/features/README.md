# Feature Layer

Feature folders should own screen-specific business flows.

Use this layer for:

- Customer flows.
- Payment flows.
- Menu flows.
- Finance and receipt review flows.
- Offline queue workflows.

Keep reusable UI primitives in `src/components`. Keep visual tokens, assets, motion, and atmosphere contracts in `src/theme`.
